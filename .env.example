PORT=5173
HOST=127.0.0.1
# Only this explicitly configured file is served. It is never copied into public/.
ROM_PATH=/absolute/path/to/game.nes
# Production container supplies these values; environment variables are not auto-loaded from this file.
# NODE_ENV=production
# APP_ROOT=/app
# CATALOG_PATH=/data/games.json
# VERIFICATION_DIR=/data/verification
# RELEASE_ID=YYYYMMDD-HHMMSS
