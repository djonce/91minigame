<web-view wx:if="showWebView" src="{{src}}" binderror="onError"></web-view><view wx:else class="page"><view class="eyebrow">PIXEL ROOM</view><view class="heading">卡带已连接。</view><view class="subtitle">可以复制链接，在浏览器中打开。</view><view wx:if="error" class="note error">{{error}}</view><view class="card card-body"><view class="card-title">游戏播放器</view><view class="line hash" selectable>{{src}}</view><button class="primary" bindtap="copyLink" disabled="{{!src}}">复制浏览器地址</button><button wx:if="local" class="secondary" bindtap="preview" disabled="{{!src}}">尝试工具内预览</button></view><view class="note"><text wx:if="local">本地联调需要电脑启动游戏服务。手机上的 127.0.0.1 不会连接到这台电脑。</text><text wx:else>微信内播放需要在小程序后台配置业务域名。请检查域名设置后重试。</text></view></view>
